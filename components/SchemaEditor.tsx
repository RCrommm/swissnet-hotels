'use client'
export default function SchemaEditor({ hotelId, hotelName, onClose }: { hotelId: string, hotelName: string, onClose: () => void }) {
  return <div>Schema Editor Placeholder for {hotelName}</div>
}
