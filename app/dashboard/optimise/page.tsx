import OptimiseWrapper from '@/components/OptimiseWrapper'

export const metadata = {
  title: 'AI Optimisation — SwissNet Hotels',
}

export default function OptimisePage() {
  return <OptimiseWrapper />
}